{{- if and .Values.encode.create .Values.multinode }}
apiVersion: leaderworkerset.x-k8s.io/v1
kind: LeaderWorkerSet
metadata:
  name: {{ include "llm-d-modelservice.encodeName" . }}
  labels:
    {{- include "llm-d-modelservice.labels" . | nindent 4 }}
    {{- include "llm-d-modelservice.encodelabels" . | nindent 4 }}
  {{- $warning := "" }}
  {{- if .Values.encode.containers }}
  {{- $firstContainer := index .Values.encode.containers 0 }}
  {{- $warning = include "llm-d-modelservice.acceleratorWarning" (dict "parallelism" .Values.encode.parallelism "resources" $firstContainer.resources "Values" .Values "container" $firstContainer) }}
  {{- end }}
  {{- if or .Values.encode.subGroupExclusiveTopology .Values.encode.annotations $warning }}
  annotations:
  {{- if .Values.encode.subGroupExclusiveTopology }}
    leaderworkerset.sigs.k8s.io/subgroup-exclusive-topology: kubernetes.io/hostname
  {{- end }}
  {{- if .Values.encode.annotations }}
    {{ toYaml .Values.encode.annotations | nindent 4 }}
  {{- end }}
  {{- if $warning }}
    llm-d.ai/accelerator-warning: {{ $warning | quote }}
  {{- end }}
  {{- end }}
spec:
  {{- if not .Values.encode.autoscaling.enabled }}
  replicas: {{ ternary .Values.encode.replicas 1 (hasKey .Values.prefill "replicas") }}
  {{- end }}
  leaderWorkerTemplate:
    size: {{ int (include "llm-d-modelservice.numWorkers" .Values.encode.parallelism) }}
    {{- if .Values.encode.subGroupPolicy }}
    subGroupPolicy:
    {{- toYaml .Values.encode.subGroupPolicy | nindent 6 }}
    {{- end }}
    workerTemplate:
      metadata:
        labels:
          {{- include "llm-d-modelservice.encodelabels" . | nindent 10 }}
        {{- if or .Values.encode.subGroupExclusiveTopology .Values.encode.podAnnotations }}
        annotations:
        {{- if .Values.encode.subGroupExclusiveTopology }}
          leaderworkerset.sigs.k8s.io/subgroup-exclusive-topology: kubernetes.io/hostname
        {{- end }}
        {{- if .Values.encode.podAnnotations }}
          {{ toYaml .Values.encode.podAnnotations | nindent 10 }}
          {{- end }}
        {{- end }}
      spec:
        {{- /* DEPRECATED; use extraConfig.hostIPC instead */ -}}
        {{- if hasKey .Values.prefill "hostIPC" }}
        hostIPC: {{ .Values.encode.hostIPC }}
        {{- end }}
        {{- /* DEPRECATED; use extraConfig.hostPID instead */ -}}
        {{- if hasKey .Values.prefill "hostPID" }}
        hostPID: {{ .Values.encode.hostPID }}
        {{- end }}
        {{- if .Values.encode.initContainers }}
        initContainers:
        {{- toYaml .Values.encode.initContainers | nindent 10 }}
        {{- end }}
        {{- if hasKey .Values.prefill "enableServiceLinks" }}
        enableServiceLinks: {{ .Values.encode.enableServiceLinks }}
        {{- end }}
        {{- if hasKey .Values.prefill "terminationGracePeriodSeconds" }}
        terminationGracePeriodSeconds: {{ .Values.encode.terminationGracePeriodSeconds }}
        {{- end }}
        {{- (include "llm-d-modelservice.modelPod" (dict "role" "encode" "pdSpec" .Values.prefill "Values" .Values "Release" .Release "Chart" .Chart)) | nindent 6 }}
        {{- with .Values.encode.containers }}
        containers:
          {{- range . }}
        {{- (include "llm-d-modelservice.container" (dict "role" "encode" "container" . "parallelism" $.Values.encode.parallelism "Values" $.Values "Release" $.Release "Chart" $.Chart "pdSpec" $.Values.prefill)) | nindent 8 }}
          {{- end }}
        {{- end }}
        {{- if .Values.encode.nodeSelector }}
        nodeSelector:
          {{- toYaml .Values.encode.nodeSelector | nindent 10 }}
        {{- end }}
        {{- if .Values.encode.tolerations }}
        tolerations:
          {{- toYaml .Values.encode.tolerations | nindent 10 }}
        {{- end }}
{{- end }}
